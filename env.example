# Power Automate Webhook Configuration
# Copy this file to .env and add your actual webhook URL

# Your Power Automate webhook URL for Teams notifications:
# This will send notifications to Teams when clients check in
# Replace with your actual webhook URL from Power Automate
REACT_APP_TEAMS_WEBHOOK_URL=https://your-webhook-url-here

# Database Configuration
# IMPORTANT: For production deployment, you MUST set up a PostgreSQL database
# 
# Option 1: Use Vercel Postgres (Recommended)
# 1. Go to your Vercel project dashboard
# 2. Go to Storage tab
# 3. Create a new Postgres database
# 4. Copy the connection string and set it as DATABASE_URL in Vercel environment variables
#
# Option 2: Use external PostgreSQL service (Supabase, PlanetScale, etc.)
# Replace with your PostgreSQL connection string:
DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# For local development, create a .env.local file with:
# DATABASE_URL="file:./dev.db"

# Note: SMS functionality has been removed from this application
# All notifications are handled through the Teams webhook above
